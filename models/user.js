const mongoose=require("mongoose");

const passport_mongoose_schema=require("passport-local-mongoose");



const userschema=new mongoose.Schema({
    email:{
        type:String,
        required:true,
    }
})
userschema.plugin(passport_mongoose_schema);

module.exports=mongoose.model("User",userschema);






























































































